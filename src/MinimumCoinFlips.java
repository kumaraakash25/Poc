public class MinimumCoinFlips {
    public static void main(String[] args) {
        String sequence = "HHTHT";
        char[] arr = sequence.toCharArray();
        boolean isTailsTraversed = false;
        boolean isHeadsTraversed = false;
        for (int i = 0; i < arr.length - 1; i++) {
            if (arr[i] == 'H' && !isTailsTraversed) {

            }
        }

    }
}
